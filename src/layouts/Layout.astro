---
interface Props {
    title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <meta name="generator" content={Astro.generator} />
        <title>{title}</title>
        <script is:inline>let FF_FOUC;</script>
    </head>
    <body class="bg-solar-base-04">
        <slot />
    </body>
</html>

<style is:global is:inline>
    @tailwind components;

    @layer components {
        .animate-ease-in-out {
            transition:
                background-color 500ms ease-in-out,
                color 500ms ease-in-out;
        }
    }

    @font-face {
        font-family: "codenew";
        src: url("/fonts/CodeNewRomanNerdFontMono-Regular.otf")
            format("opentype");
        font-weight: normal;
        font-style: normal;
        font-display: swap;
    }
    @font-face {
        font-family: "codenew-bold";
        src: url("/fonts/CodeNewRomanNerdFontMono-Bold.otf") format("opentype");
        font-weight: normal;
        font-style: normal;
        font-display: swap;
    }
    @font-face {
        font-family: "codenew-italic";
        src: url("/fonts/CodeNewRomanNerdFontMono-Italic.otf")
            format("opentype");
        font-weight: normal;
        font-style: normal;
        font-display: swap;
    }
</style>
