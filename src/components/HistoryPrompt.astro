---
export const prerender = true;

import { bolt, pulse } from "../icon.ts";

interface Props {
    buffer: string;
}

const start = Date.now();
const { buffer } = Astro.props;
---
<div class="flex flex-wrap gap-2 items-center">
    <p class="text-lack-gray9">λ {buffer}</p>
</div>
